import type { Metadata } from "next";
import { QuantityWorkspace } from "@/components/quantity-workspace";
export const metadata: Metadata = { title: "Cubicaciones | BIM + IA" };
export default function QuantitiesPage() { return <QuantityWorkspace/>; }
